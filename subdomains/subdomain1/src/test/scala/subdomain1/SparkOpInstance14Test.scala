import org.scalatest.funsuite.AnyFunSuite
import subdomain1.SparkOpInstance14

class SparkOpInstance14Test extends AnyFunSuite {
  test("SparkOpInstance14: name should be nu-br/dataset/spark-op-instance-14") {
    assert(SparkOpInstance14.name == "nu-br/dataset/spark-op-instance-14")
  }

  // Additional tests for inputs, query, metadata, and runConfigurations can be added here
}
